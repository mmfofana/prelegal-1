services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - prelegal-data:/app/data
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/prelegal.db

volumes:
  prelegal-data:
